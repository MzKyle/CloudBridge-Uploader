# 环境依赖

## 开发

- Node.js 20
- npm 9+
- Linux 或 Windows
- 可访问 GitHub 和 npm registry

## 运行

- 至少一个对象存储 bucket
- Aliyun OSS 或 S3-compatible 访问凭据
- 本地 Source Root 可读
- 若启用 cleanup，本地 Source Root 所在磁盘必须允许删除已完成目录

## 打包

Linux 打包当前支持 AppImage x64 和 deb x64。Windows 打包当前支持 NSIS x64。
